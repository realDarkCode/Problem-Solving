// Toph - Take It Easy
#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    int n;
    cin >> n;
    long long a, b, c, d;
    a = ((n - 1) * (n - 2)) / 2;
    b = n * n;
    d = pow(2, n) + pow(3, n);
    return 0;
}